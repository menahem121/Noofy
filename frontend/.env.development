# When running the browser dev server directly (not via `npm run tauri:dev`),
# start the backend with a fixed port: NOOFY_BACKEND_PORT=8000 python -m app
# The Vite proxy forwards /api → http://127.0.0.1:8000
VITE_DEV_BACKEND_PORT=8000
